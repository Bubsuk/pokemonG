#pragma once
// 미리 컴파일된 헤더를 만들어 보자(빌드 속도가 빨라진다.)
// 일반적으로 이름은 stdafx
// 프로젝트의 속성 -> C/C++ -> 미리 컴파일된 헤더 -> 사용
// stdafx.cpp의 속성 -> 미리 컴파일 된 헤더 -> 만들기
// 미리 컴파일된 헤더를 사용하려면 모든 cpp에 #include "stdafx.h"  를 추가해야한다.

#include <string>
#include <vector>
#include <unordered_map>

#include <Windows.h>

using namespace std;

